# 磨き込み改修 作業メモ（2026-06-12 開始）

出題者視点レビューの指摘6点＋機能2点をすべて実施する。**docxマスター自体を改修**し、
アプリ・問題編・検証チェックリストの整合を保つ。先生の販売前照合は未着手のため、
チェックリストは改修後の内容で再生成する（改修内容列つき＝先生が変更点を追える）。

## 指摘と対応
1. 正解がイに72%集中（訴訟選択は20/20）→ 決定論的シャッフルで ア〜エ 各テーマ均等化。解説内の肢記号も同時リマップ
2. 正解肢が最長80%（平均+21.6字）→ 誤り肢に理由句を加筆して長さ均し（内容パス）
3. 「正しいものはどれか」93% → 各テーマ3〜4問を「誤っているものはどれか」型に改題
4. 事例型が5/260のみ → 要件事実に事例型10問新設（問25〜34）
5. 断定語が誤り肢に集中16問 → 誤り肢の断定語を緩和/正解肢にも配置（内容パス）
6. 解説が正解肢のみ113問 → 全肢を潰す解説に増強（内容パス）
7. 機能：模試モード（30問抽出・科目配分・タイマー120分・一括採点・6割判定）
8. 機能：弱点復習（誤答した問題だけ再演習）

## パイプライン（作業ファイル）
- `_export_json.py`：13テーマの解答解説編docx → `_workdata/<key>.json`
- `_shuffle_balance.py`：JSONの肢順を決定論的に均等化＋解説の記号リマップ（→ `_workdata/<key>.shuffled.json`）
- 内容パス：Claudeが `<key>.shuffled.json` を直接編集（指摘2/3/5/6＋要件事実の新問）→ `<key>.final.json`
- `_generate_docx.py`：final.json → `*_解答解説編.docx`＋`*_問題編.docx` を再生成（正解一覧表つき・原本は `_backup_改修前_20260612\` へ）
- `更新（再生成）.bat` 相当＝ `py _build_quiz.py` でHTML再生成
- `_analyze_quiz.py` 再実行で指標確認（正解分布・最長率・断定語）
- `_template.tpl` に模試モード＋弱点復習を追加
- 検証チェックリストxlsx再生成（改修内容列つき）
- 引き継ぎ書更新

## 進捗（2026-06-12 全完了）
- [x] _export_json.py で13テーマ抽出
- [x] _shuffle_balance.py でシャッフル＋リマップ（均等数×ランダム順＝周期性なし）
- [x] 内容パス 13テーマ完了（要件事実は事例型10問新設で24→34問。総計270問）
- [x] _generate_docx.py で docx 再生成（原本は _backup_改修前_20260612\ に保存）
- [x] HTML再生成＋指標再分析（正解分布69/67/68/66・最長率80→61%・断定語16→11・誤り選び10→29）
- [x] 模試モード実装（弱点復習は既存機能と判明＝確認のみ）
- [x] 動作確認（http.server 8123＋ブラウザ実機：模試30問→採点→合否→誤答レビュー→記録、通常モード非破壊、コンソールエラーなし。テスト記録は消去済み）
- [x] チェックリストxlsx再生成（検証チェックリスト_270問.xlsx・参照整合チェック済）
- [x] 引き継ぎ書更新（§0+）

## 残り（先生の作業）
- GitHubへ アップロード用\index.html をアップ（引き継ぎ書§3の手順）
- 販売前照合（検証チェックリスト_270問.xlsx。橙=改題・黄=新設の行を重点的に）

## 注意（罠）
- 解説の記号リマップは「カタカナに隣接しないア/イ/ウ/エ」のみ置換（イメージ・ケア等の語中誤爆防止）。プレースホルダ2段置換で衝突回避
- 組合せ問題の中身ラベル（い・ろ・は／①②／ａ〜ｄ）はリマップ対象外＝アイウエ以外なので安全。stem/choices内に裸のアイウエがあれば要手動確認としてフラグ
- 問題編docxも同時再生成（問題と肢は解答解説編と完全同期）
- 誤り肢の加筆は「誤りのまま」を厳守。改題で肢を正しい記述に直すときは法的正確性に最大注意（最終ゲートは先生の照合）
